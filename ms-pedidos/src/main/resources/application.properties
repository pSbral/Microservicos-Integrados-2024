spring.application.name=ms-pedidos

spring.profiles.active=test
spring.jpa.open-in-view=false

# É preciso passar a configuração eureka.client.serviceUrl.defaultZone
# e o localhost 8081, onde o Eureka Server está recebendo as requisições
eureka.client.serviceUrl.defaultZone=http://localhost:8081/eureka

# adicionar para testar subir mais de uma instância do MS
# variável de ambiente para definir um identificador único da instância
# nome da instância ${spring.application.name}
# gerar um número aleatório inteiro ${random.int}

# eureka.instance.instance-id=${spring.application.name}:${random.int}

# define a porta para o Eureka controlar em qual porta ele vai subir o serviço
server.port=0

